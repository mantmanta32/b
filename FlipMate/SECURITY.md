# Security
API credentials are never committed, logged, or embedded in builds. REAL mode must be explicitly enabled after credential validation. Withdrawal permission must remain disabled. This application is not financial advice.
